from django.urls import path
from rest_framework.routers import DefaultRouter

# For now, just empty URLs - you can add API views later
urlpatterns = [
    # Add client API endpoints here when needed
]